import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('hasan masrur'),
      ),
      body: Container(
        height: 500,
        color: Colors.black,
        child: Text('hasan'),
      ),
    );
  }
}
